# 2026-03-02

- 用户明确要求：non-gateway 的大模型调用使用 `kimi-k2.5`（上下文比 8k 大很多），不要用“截断输入”来规避 token limit。
- 发现 kimi-k2.5 的约束：`temperature` 只能设为 `1`，否则返回 `invalid temperature: only 1 is allowed for this model`；已在 `content-pipeline/src/generator/content_generator.py` 针对 kimi-k2.5 强制 temperature=1（commit 854cdc3）。
- /discover 采集任务需要可终止：已在 runs 列表增加“终止”按钮、后端 `POST /discover/api/run_cancel`，并在 `discovery_runs` 增加 pid 字段记录子进程；但历史 run 可能 pid 为 null 导致 400（已改为 pid 缺失时仅标记 cancelled），并从日志自动识别失败把 status 置为 failed（commits 9e133dd, c6e76ec）。
- 用户反馈“查看没反应”：后端其实有收到 run_status/items 请求，怀疑 Feishu 内置浏览器缓存导致页面 JS 旧；已对 /discover 禁用缓存并增加点击即时反馈（commit cef2563）。
- 关键故障链路：discover_collect 在第 3 个 query 的 analyze_fit 里触发 token limit 8192/18099 并退出；此前 run 会显示 running 但实际已失败（现已从 log 识别）。
- Feishu bot 权限仍报缺：缺 `contact:contact.base:readonly`，需要管理员授权链接：https://open.feishu.cn/app/cli_a914da88ae391cbb/auth?q=contact:contact.base:readonly
